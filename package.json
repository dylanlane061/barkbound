{
  "name": "barkbound",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev -w packages/web\" \"npm run dev -w packages/api\"",
    "build": "npm run build -w packages/pawsignal && npm run build -w packages/api && npm run build -w packages/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
